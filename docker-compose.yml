version: '3'
services:
  pwa:
    build:
      context: ./etc/pwa/
    ports: 
    - 3000:3000
  web:
    build:
      context: ./etc/
      dockerfile: web.Dockerfile
    ports:
      - ${WEBPORT}:80
  admin:
    build:
      context: ./etc/admin/
    expose:
      - "9000"
    # volumes:
    #   - ./etc/admin/app:/app:rw
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    # volumes: 
    #   - ./var/db:/var/lib/mysql:rw
    ports:
      - 33060:3306
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
